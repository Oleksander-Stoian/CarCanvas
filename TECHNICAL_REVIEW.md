# CarCanvas Project: Technical Review Documentation

## 1. Executive Summary

**CarCanvas** — це веб-додаток (SPA) для моделювання та аналізу геометричних перетинів у 2D просторі. Проєкт розроблено як Proof of Concept (PoC) для демонстрації можливостей **Blazor WebAssembly** у вирішенні задач обчислювальної геометрії (Computational Geometry) безпосередньо у браузері (Client-side).

### 1.1. Project Goals & Scope

Основна мета — створити інтерактивний інструмент, який дозволяє користувачеві:
*   Завантажувати та візуалізувати карту перешкод (10 000+ ліній у базовому сценарії; протестовано також на значно більших сценах — див. Performance Benchmarks).
*   Керувати положенням та орієнтацією транспортного засобу (Car).
*   Отримувати миттєвий зворотний зв'язок про точки перетину (Collision Detection) з точністю до пікселя.

### 1.2. Technology Stack

Рішення побудовано на сучасному стеку Microsoft:
*   **Platform**: .NET 8 (ASP.NET Core hosted).
*   **Framework**: Blazor WebAssembly (WASM) — виконання C# коду в браузері.
*   **Rendering**: HTML5 Canvas API (через оптимізований JS Interop шар) для високошвидкісного малювання.
*   **Architecture**: Clean Architecture + MVVM.

### 1.3. Compliance with Requirements

Проєкт повністю реалізує функціональні вимоги технічного завдання (ТЗ):
1.  **Rendering**: Відображення масиву точок (Car) та ліній (Obstacles).
2.  **Transformation**: Підтримка `Translate` (X, Y) та `Rotate` (Angle) відносно центру мас.
3.  **Algorithm**: Реалізація власного алгоритму перетину (без використання готових фізичних рушіїв).
4.  **Optimization**: Цільова вимога: інтерактивний режим на сценах 10k+ ліній. Фактичні метрики залежать від сцени/пристрою і відображаються в UI; підтверджені stress-тести наведені в розділі Performance Benchmarks.
5.  **UX**: Візуалізація точок перетину (червоні маркери) та лічильника.

Цей документ детально описує архітектурні рішення, алгоритмічну базу та стратегії оптимізації, що дозволили досягти цільових показників продуктивності.

## 2. Architecture & Design Principles

Проєкт побудовано на основі принципів **Clean Architecture** з чітким розділенням на шари. Це забезпечує незалежність бізнес-логіки від UI-фреймворку та деталей інфраструктури, що є критичним для тестування та підтримки.

### 2.1. Layered Structure (Clean Architecture)

Архітектура складається з чотирьох основних шарів, кожен з яких має чітку відповідальність:

*   **Domain (`CarCanvas.Domain`)**: Ядро системи. Містить чисті сутності та Value Objects (`Point2D`, `LineSegment`, `CarModel`, `Transform`, `Aabb`). Цей шар не має зовнішніх залежностей і визначає незмінні правила предметної області.
*   **Application (`CarCanvas.Application`)**: Визначає сценарії використання. Тут знаходяться інтерфейси сервісів (`IIntersectionService`, `ICanvasSceneService`), DTO (`IntersectionResult`, `AppOptions`) та алгоритми, що є частиною бізнес-правил (наприклад, `UniformGridIndex`). Цей шар оркеструє потоки даних.
*   **Infrastructure (`CarCanvas.Infrastructure`)**: Реалізація деталей. Тут зосереджена "важка" логіка: реалізація пошуку перетинів (`IntersectionService`), низькорівневі геометричні алгоритми (`GeometryUtils`, `Bresenham`) та робота з файлами. Цей шар задовольняє контракти, визначені в Application.
*   **Web (`CarCanvas.Web`)**: Точка входу (Blazor WebAssembly). Відповідає виключно за візуалізацію, введення даних користувачем та композицію компонентів (Composition Root).

### 2.2. MVVM Pattern in Blazor

Для управління станом UI використано патерн **MVVM (Model-View-ViewModel)**, що дозволяє відокремити логіку відображення від бізнес-логіки:

*   **View (`CanvasView.razor`, `Home.razor`)**:  Пасивні компоненти (presentational components), що містять лише розмітку та прив'язку до даних (Data Binding). Вони не містять логіки обробки даних.
*   **ViewModel (`DashboardViewModel`)**: Утримує стан екрану (`Car1`, `Lines`, `IsLoading`) та команди. ViewModel виступає посередником: вона викликає сервіси Application/Infrastructure, оновлює свій стан і сповіщає View про зміни через механізм `NotifyStateChanged`.
*   **State Management**: Завдяки MVVM, стан додатку не розмазаний по компонентах, а централізований, що спрощує налагодження та тестування поведінки UI.

### 2.3. Separation of Concerns & Threading Model

Ключовим архітектурним рішенням є чітке розмежування відповідальностей, особливо в контексті продуктивності:

*   **Pure Logic vs Threading**: Сервіс `IntersectionService` спроєктовано як синхронний та "чистий" (pure computation). Він не знає про потоки чи `Task`. Відповідальність за асинхронність (`Task.Run`) винесена на рівень **ViewModel**. Це гарантує, що бізнес-логіка проста для тестування, а ViewModel ізольовано керує запуском обчислень і станом UI. У поточному WASM Single Thread обчислення все одно блокують UI під час виконання (див. розділ 4).
*   **Math vs Rendering**: Геометричні розрахунки виконуються в абстрактній математичній системі координат (де Y направлений вгору, як у декартовій системі). Шар візуалізації (`CanvasSceneService`) відповідає за трансформацію цих координат у простір екрану (де Y направлений вниз) безпосередньо перед малюванням.
*   **Abstraction of JS Interop**: Прямі виклики JavaScript інкапсульовані в `CanvasSceneService`, що дозволяє решті C# коду працювати без знання про специфіку браузера.

## 3. Algorithmic Core: Intersection Engine

Ядро системи базується на двофазному підході до виявлення перетинів (Two-Phase Intersection Detection), що дозволяє ефективно обробляти сцени з великою кількістю об'єктів (10k+ ліній). Алгоритм поділяється на **Broad Phase** (грубий відбір кандидатів) та **Narrow Phase** (точна перевірка).

### 3.1. Broad Phase: Spatial Partitioning

Для швидкого відсіювання ліній, які гарантовано не перетинають машину, використовується **Uniform Grid** (рівномірна сітка).

*   **UniformGridIndex**: Простір розбивається на клітинки фіксованого розміру. Кожна клітинка зберігає список індексів ліній, що проходять через неї. Це дозволяє замінити перевірку "всіх з усіма" (O(N)) на перевірку лише тих ліній, що знаходяться поруч з машиною (локальний пошук O(1) у середньому випадку).
*   **Grid Traversal (DDA)**: Для додавання ліній у сітку використовується алгоритм **Amanatides & Woo (DDA)**. Він дозволяє ефективно знаходити всі клітинки, які перетинає відрізок, рухаючись вздовж променя. Це точніше і швидше, ніж перевірка AABB лінії проти AABB клітинок.
*   **Candidate Selection**: Під час запиту на перетин, ми беремо AABB машини (bounding box), знаходимо всі клітинки сітки, які він покриває, і об'єднуємо списки ліній з цих клітинок. Отриманий набір — це "кандидати" для точної перевірки.

### 3.2. Narrow Phase: Pixel-Perfect Precision

Кандидати, відібрані на попередньому етапі, проходять точну перевірку на піксельному рівні.

*   **PixelSet Rasterization**: Форма машини є складною і може бути повернута на довільний кут. Замість аналітичної перевірки полігонів, ми використовуємо підхід растеризації. Машина перетворюється на **PixelSet** — `HashSet<int>`, що містить упаковані координати (Y * Stride + X) усіх пікселів, які займає машина.
*   **Liang-Barsky Clipping**: Перед тим як перевіряти кожен піксель лінії, ми застосовуємо алгоритм відсікання Ляна-Барскі. Він дозволяє швидко знайти частину лінії, що знаходиться всередині AABB машини. Якщо лінія повністю поза межами AABB, вона відкидається. Це суттєва оптимізація, що економить час на ітерації пікселів.
*   **Bresenham's Algorithm**: Для перевірки перетину відсіченої частини лінії з машиною використовується алгоритм Брезенхема. Ми ітеруємо пікселі лінії та перевіряємо їх наявність у `PixelSet` машини.
*   **Zero-Allocation Optimization**: Реалізація Брезенхема оптимізована для уникнення алокацій пам'яті (`yield return` замінено на цикл `while` всередині методу `CountHitsOnLine`). Це критично для зниження навантаження на Garbage Collector при обробці тисяч ліній.

### 3.3. Caching Strategy

Побудова `PixelSet` для машини — відносно дорога операція (трансформація точок + растеризація).

*   **Lazy Caching**: Ми використовуємо `ConcurrentDictionary<int, Lazy<...>>` для кешування побудованого `PixelSet`.
*   **TransformHash**: Ключем інвалідації кешу є хеш від параметрів трансформації (X, Y, Rotation). Якщо машина не рухалась, ми миттєво отримуємо готовий `PixelSet`.
*   **Single-Flight**: Використання `LazyThreadSafetyMode.ExecutionAndPublication` гарантує, що навіть при одночасних запитах з різних потоків, побудова `PixelSet` для конкретного стану машини відбудеться лише один раз.

### 3.4. Execution Modes (Fast vs Full)

Система підтримує два режими роботи для покращення UX:

*   **Fast Mode (Early Stop)**: Пріоритет швидкості. Пошук зупиняється, як тільки знайдено достатню кількість точок перетину для візуалізації (ліміт `MaxMarkersToDraw`). Це забезпечує миттєвий фідбек користувачеві.
*   **Full Mode**: Пошук виконується до кінця, щоб підрахувати точну загальну кількість перетинів (`TotalHits`). Це важливо для аналітики та точних метрик, але може займати більше часу.

## 4. Concurrency & UI Responsiveness (Current Limitations)

У поточному середовищі Blazor WebAssembly (Single Thread), виконання важких обчислень має суттєві обмеження, які важливо розуміти.

### 4.1. Browser Main Thread Blocking

*   **Single Thread Execution**: Весь C# код та рендеринг UI виконуються в одному головному потоці браузера.
*   **Blocking Behavior**: Метод `FindIntersections` є CPU-bound операцією. Під час його виконання потік повністю блокується. Це означає, що UI **не оновлюється**, анімації зупиняються, і користувач не може взаємодіяти з інтерфейсом до завершення розрахунку.
*   **Task.Run Limitations**: Використання `await Task.Run(...)` у ViewModel дозволяє UI-потоку оновити стан (наприклад, показати спіннер) *перед* початком роботи, але не запобігає "замерзанню" під час самого обчислення. У середовищі WASM без Web Workers `Task.Run` не забезпечує справжнього паралелізму для CPU-bound задач.

### 4.2. Thread-Safe Architecture (Future Proofing)

Незважаючи на однопотокове виконання в поточній версії, архітектура спроєктована як Thread-Safe для майбутнього масштабування:

> **Note**: Thread-safety primitives (`ConcurrentDictionary`, `Lazy<T>`) are currently redundant in single-threaded WASM and are kept purely for portability to future Server/WebWorker execution models.

*   **ConcurrentDictionary**: Кеш `PixelSet` реалізовано через `ConcurrentDictionary`. Це гарантує цілісність даних навіть якщо сервіс буде викликатися паралельно.
*   **Single-Flight Execution**: Використання `Lazy<T>` з режимом `ExecutionAndPublication` вирішує проблему "Thundering Herd".
*   **Immutability**: Ключові структури даних є незмінними.

### 4.3. Future Concurrency Improvements

Для вирішення проблеми блокування UI заплановано:
*   **Web Workers**: Винесення розрахунків у справжній фоновий потік (вимагає серіалізації даних).
*   **Task.Yield()**: Розбиття довгих циклів на частини з поверненням керування UI-потоку (Time-slicing), щоб браузер залишався чуйним (хоча це збільшить загальний час виконання).

## 5. Performance Optimizations & Memory Management

Оскільки система повинна обробляти десятки тисяч ліній (300k+) у реальному часі та виконувати перевірку перетинів з точністю до пікселя, питання продуктивності розглядалися як першочергові. Ми відмовилися від наївних рішень на користь оптимізованих алгоритмів обчислювальної геометрії.

### 5.1. Identified Bottlenecks

Під час початкового профілювання було виявлено три критичні проблеми:

*   **Brute-force перевірка**: Перевірка "кожен з кожним" (O(N)) для 10 000 ліній створювала помітні затримки, що було неприпустимо для інтерактивної взаємодії.
*   **Растеризація довгих ліній**: Виклик алгоритму Брезенхема для лінії довжиною 2000 пікселів генерує 2000 ітерацій перевірки. Якщо лінія лише торкається bounding box машини, 95% цих ітерацій є марними обчисленнями.
*   **Hot Paths Allocation**: Використання `yield return` та LINQ у методах генерації точок створювало надмірний трафік пам'яті (GC pressure) у найгарячішому циклі програми ("Narrow Phase").

### 5.2. Implemented Optimizations

Для вирішення цих проблем було впроваджено каскад оптимізацій:

*   **Spatial Indexing (UniformGridIndex)**:
    *   Використано просторове хешування (Spatial Hashing) через рівномірну сітку.
    *   Лінії додаються у клітинки сітки за допомогою алгоритму обходу **Amanatides & Woo (DDA)**, що гарантує точність покриття.
    *   *Результат*: Замість перевірки 10 000 ліній, алгоритм перевіряє лише ті 20-50 кандидатів, що потрапляють у клітинки, перекриті машиною.

*   **Liang-Barsky Clipping**:
    *   Перед запуском "важкого" попіксельного перебору, ми застосовуємо алгоритм відсікання ліній Ляна-Барскі відносно AABB машини.
    *   Це дозволяє миттєво відкинути лінії, що проходять повз (Trivial Rejection), та обрізати довгі лінії лише до тієї ділянки, що потенційно може перетинати машину.

*   **Zero-Allocation Bresenham**:
    *   Метод `GetPointsOnLine` (який повертав `IEnumerable<Point2D>`) було замінено на спеціалізований метод `CountHitsOnLine`.
    *   Він не повертає колекцію точок, а приймає `PixelSet` машини та лічильник.
    *   Цикл реалізовано на чистих стекових змінних (`int`), без створення об'єктів-ітераторів.

*   **PixelSet Caching & Invalidation**:
    *   Побудова `PixelSet` (растеризація форми машини) — це найдорожча операція (CPU-bound).
    *   Ми кешуємо результат у `ConcurrentDictionary` з використанням **TransformHash** (хеш від X, Y, Rotation).
    *   Якщо машина не рухається (хеш не змінився), час підготовки складає 0 мс.

*   **JS Interop Batching**:
    *   Малювання примітивів виконується пакетами (batch), що мінімізує оверхед переходів між WASM та JS.

*   **Early Stop (Fast Mode)**:
    *   Як тільки знайдено ліміт перетинів (`MaxMarkersToDraw`), пошук переривається. Це критично для UI, коли користувач швидко перетягує об'єкти і йому потрібен миттєвий візуальний фідбек.

*   **Render Budget / Visual Decimation**:
    *  Для сцен, що містять понад 50 000 рядків, рендерер автоматично переходить у спрощений режим, в якому малюється лише кожен N-й рядок (розрахований на ~20 000 видимих рядків), щоб зберегти чуйність інтерфейсу користувача.
    *   Коли цей режим активний, відображається значок інтерфейсу користувача «Спрощене рендеринг (режим 300k)».

### 5.3. Memory Management

Для підтримки стабільної роботи Garbage Collector (GC) у середовищі WebAssembly (де GC може бути повільнішим):

*   **Struct Usage**: `Point2D` оголошено як `record struct`. Це забезпечує семантику значень (Value Type) та розміщення на стеку, уникаючи оверхеду заголовків об'єктів у кучі (Heap).
*   **Removal of Allocations**: У "гарячому шляху" (Narrow Phase) повністю усунуто алокації посилальних типів. Використовуються лише примітиви (`int`, `bool`) та структури.
*   **Reuse of Collections**: Списки кандидатів та буфери перетинів використовуються повторно або створюються з попередньо визначеною ємністю (`Capacity`), щоб уникнути зайвих ресайзів.

### 5.4. Runtime Profiling (Telemetry)

Об'єкт `IntersectionResult` містить детальні метрики для кожного запиту, що дозволяє контролювати регресії продуктивності в реальному часі:

*   `GridBuildMs`: Час побудови UniformGridIndex (при генерації/завантаженні ліній).
*   `RenderMs`: Час рендеру сцени (JS draw).
*   `TotalCalcMs`: Сумарний час розрахунку (FindIntersections).
*   `NarrowPhaseMs`: Час, витрачений на безпосередню перевірку пікселів (Liang-Barsky + Bresenham).

Ці метрики відображаються в UI і демонструють ефективність алгоритму: час обробки "Narrow Phase" зазвичай є незначним порівняно з часом рендерингу.
*   **Performance Benchmark**: Типовий час виконання Narrow Phase на демо-сцені складає **< 5 мс** (без врахування рендерингу). Фактичне значення залежить від пристрою і відображається в UI.

### Recorded Stress Benchmark (300k Lines)
*   **Scene**: 300,000 random lines generated from UI.
*   **Fast Mode**: total time ≈ 3.3s, NarrowPhaseMs ≈ 3110ms, early stop at MaxMarkersToDraw = 200,000 (UI: TotalHits = 200,000).
*   **Full Mode**: total time ≈ 8.1s, NarrowPhaseMs ≈ 7931ms, full count TotalHits = 544,943.
> **Note**: Значення залежать від пристрою/браузера; наведено як зафіксований приклад з UI.

## 6. Critical Limitations & Engineering Analysis

Цей розділ чесно описує технічні обмеження поточної реалізації (PoC) та компроміси, на які пішов розробник.

### 6.1. Platform Limitations
*   **UI Freezing**: На великих сценах (десятки тисяч+ ліній) час розрахунку може перевищувати сотні мілісекунд і викликати помітний UI-freeze; у stress-тесті 300k це може бути секунди і браузер може показати "Page Unresponsive".
*   **Canvas Performance**: HTML5 Canvas 2D API має межу продуктивності при малюванні >100k об'єктів у кожному кадрі.

### 6.2. Implemented UX Safeguards

Для забезпечення чутливості інтерфейсу (responsiveness) навіть під час важких обчислень реалізовано наступні механізми:

*   **Debouncing**: У `DashboardViewModel.UpdateTransformAsync` реалізовано затримку **100 ms**. Якщо користувач продовжує рухати слайдер, попередній запит скасовується, і розрахунок не запускається.
*   **Cancellation**:
    *   Використовується `CancellationTokenSource` на рівні ViewModel.
    *   Токен передається через усі шари до `IntersectionService` та алгоритму Брезенхема.
    *   Перевірка `ct.ThrowIfCancellationRequested()` виконується:
        *   У циклі перебору кандидатів (кожні 100 ітерацій).
        *   У циклі растеризації (кожні 50 кроків).
    *   Це дозволяє миттєво зупинити застарілий розрахунок, як тільки надходять нові дані.

### 6.3. Memory Constraints & Trade-offs
*   **PixelSet Overhead**: Для швидкості O(1) ми використовуємо `HashSet<int>`. Це створює значний оверхед пам'яті (≈20-30 байт на піксель).
*   **Risk**: Велика машина (наприклад, 1000x1000 пікселів) може викликати Memory Spike, що є критичним для WASM (обмеження пам'яті браузера).
*   **Alternatives Considered**: `BitArray` або `bool[,]` споживали б менше пам'яті, але `HashSet` був обраний для простоти реалізації розріджених даних у PoC.

### 6.4. Known Limitations & Stability Risks

*   **Unbounded Input**: Відсутні жорсткі ліміти на кількість ліній або координати.
*   **Crash Risk**: Завантаження файлу з 1 млн ліній, використання координат `int.MaxValue`, від’ємних розмірів або `self-intersecting shapes` може призвести до крашу вкладки браузера через вичерпання пам'яті або переповнення стеку.

### 6.5. Error Handling Transparency
*   **Current Behavior**: Критичні помилки в алгоритмі або при парсингу файлу можуть призвести до неперехоплених винятків, які видно лише в консолі браузера.
*   **Planned**: Централізована обробка помилок (Global Error Boundary) та виведення повідомлень користувачеві (Toasts/Alerts).

## 7. Quality Assurance & Testing Strategy

Забезпечення якості у проєкті реалізовано через багаторівневу стратегію тестування, яка покриває як коректність окремих алгоритмів, так і стабільність системи при граничних навантаженнях.

### 7.1. Unit Testing (xUnit)

Базовий рівень тестування (`CarCanvas.Tests`) сфокусований на ізольованій перевірці критичних компонентів:

*   **Algorithmic Correctness**: Тести для `Bresenham` та `LiangBarsky` перевіряють генерацію точок на відомих примітивах (горизонтальні, вертикальні, діагональні лінії).
*   **Geometry Transformations**: Перевірка класу `PointTransformer` гарантує, що перетворення координат (Y-up ↔ Y-down) відбувається без спотворень.
*   **Intersection Logic**: Тести для `IntersectionService` перевіряють базові сценарії перетинів (лінія перетинає машину, лінія проходить повз, лінія всередині).

### 7.2. Invariant Testing (Algorithm Verification)

Оскільки ми використовуємо оптимізовані алгоритми (Fast Mode, Spatial Hashing), існує ризик, що оптимізація може змінити результат. Для цього застосовується підхід **Invariant Testing**:

*   **Baseline Comparison**: Результати роботи оптимізованого `IntersectionService` (з Uniform Grid та Liang-Barsky) порівнюються з результатами "наївного" Brute-force алгоритму.
*   **Rule**: `Result(Optimized) == Result(BruteForce)` є інваріантом, який має виконуватися завжди (за винятком Fast Mode, де ми зупиняємось раніше).

### 7.3. Randomized Testing (Fuzzing)

Для виявлення рідкісних помилок (Edge Cases), які важко передбачити вручну, ми використовуємо генерацію випадкових сцен (`RandomSceneFactory`):

*   **Stress Testing**: Система бомбардується тисячами випадкових ліній з різними параметрами (довжина, кут, позиція).
*   **Corner Cases**: Генератор спеціально створює "незручні" ситуації:
    *   Лінії нульової довжини (точки).
    *   Лінії, що проходять точно по межі клітинок сітки (Grid boundary issues).
    *   Паралельні лінії, що накладаються одна на одну.

### 7.4. Manual QA & Visual Verification

Оскільки кінцевим результатом є візуалізація, візуальне тестування є невід'ємною частиною QA:

*   **Debug Overlay**: У додатку реалізовано режим **"Show Debug Boxes"**, який візуалізує:
    *   Межі AABB (Axis-Aligned Bounding Box) для машин і ліній.
    *   Клітинки Uniform Grid, які перевіряються алгоритмом.
    *   Точки перетину (червоні маркери).
*   **UI Responsiveness Check**: Перевірка поведінки UI при навантаженні (див. Limitations).

### 7.5. Future Improvements

*   **BenchmarkDotNet**: Планується додавання автоматизованих бенчмарків для контролю регресій продуктивності при змінах у коді (наприклад, при оновленні .NET).
*   **E2E Testing (Playwright)**: Для автоматизації перевірки користувацьких сценаріїв у браузері.

## 8. Coordinate Systems & Transformations

Коректна робота з системами координат є фундаментом геометричного ядра. У проєкті використовуються два основні простори, і чітке розуміння їх взаємодії є критичним для уникнення візуальних артефактів та помилок у розрахунках.

### 8.1. Mathematical vs Screen Coordinates

*   **Mathematical Space (World)**: Використовує стандартну декартову систему, де вісь Y направлена **вгору**. Це "рідна" система координат для всієї бізнес-логіки, імпорту даних та алгоритмів перетину.
*   **Screen Space (Canvas)**: Відповідає стандарту HTML5 Canvas, де вісь Y направлена **вниз** (початок координат (0,0) у верхньому лівому куті).

*   **Inversion Strategy**: Перетворення з математичної системи в екранну відбувається **лише** в момент візуалізації. Вся внутрішня логіка (`IntersectionService`, `UniformGridIndex`) працює в єдиному математичному просторі, що спрощує налагодження та тестування.

### 8.2. PointTransformer Logic

Клас `PointTransformer` є єдиним джерелом істини для геометричних перетворень. Він виконує операції у строго визначеному порядку (Scale -> Rotate -> Translate), хоча в поточній версії Scale = 1.

*   **Rotation**: Обертання відбувається навколо геометричного центру об'єкта (`CarModel.Center`), а не навколо початку координат.
    ```csharp
    // Алгоритм обертання
    double xRot = (x - cx) * cos - (y - cy) * sin;
    double yRot = (x - cx) * sin + (y - cy) * cos;
    ```
*   **Translation**: Зміщення додається після обертання.
*   **Rounding**: Оскільки екранні координати дискретні (пікселі), фінальні значення округлюються (`Math.Round(..., MidpointRounding.AwayFromZero)`). Це важливо для детермінізму — щоб одна й та сама позиція завжди давала один і той самий набір пікселів.

### 8.3. Rendering Pipeline & Consistency

Для забезпечення узгодженості між тим, що "бачить" алгоритм, і тим, що бачить користувач:

1.  **Input**: Користувач завантажує лінії (математичні координати).
2.  **Logic**: `IntersectionService` розраховує перетини, використовуючи математичні координати.
3.  **Visualization**:
    *   `CanvasSceneService` отримує математичні координати.
    *   Якщо активовано режим `CoordinateMode.MathYUp`, сервіс автоматично інвертує Y перед викликом JS Interop: `yFinal = (canvasHeight - 1) - yOriginal`.
    *   Це гарантує, що намальована лінія візуально відповідає тій, яку перевіряв алгоритм.

### 8.4. Precision & Numerical Stability

*   **Integer Grid**: Всі ключові структури (`Point2D`, `Aabb`) використовують `int`. Це свідоме рішення для уникнення проблем з плаваючою комою (floating point errors) при перевірці рівності координат.
*   **Double for Calculations**: Проміжні обчислення (обертання) виконуються в `double` для збереження точності, і лише фінальний результат приводиться до `int`.
*   **Rasterization**: При перетворенні векторної форми машини в `PixelSet` ми фактично дискретизуємо простір. Це означає, що машина займає конкретний набір цілочисельних координат. Будь-яка лінія, що проходить через ці координати, вважається такою, що перетинає машину.


## 9. Future Roadmap & Trade-offs

Жодне архітектурне рішення не є ідеальним; це завжди компроміс. У цьому розділі ми чесно аналізуємо обмеження поточної реалізації та окреслюємо шлях для масштабування системи.

> **Disclaimer**: Функції, описані в розділі Roadmap (Web Workers, WebGL), є планами на майбутнє і **не реалізовані** в поточній версії проєкту.

### 9.1. Trade-off Analysis

| Рішення | Перевага (Pro) | Недолік (Con) |
| :--- | :--- | :--- |
| **Uniform Grid Index** | O(1) доступ, проста реалізація, ідеально для рівномірного розподілу. | Неефективний при кластеризації об'єктів (worst-case O(N) в одній клітинці). Споживає багато пам'яті для порожніх клітинок при великому розрідженому просторі. |
| **PixelSet Rasterization** | Точність піксель-в-піксель для довільних форм. Швидка перевірка O(1) через HashSet. | Повільна побудова (хоча кешується). Велике споживання пам'яті для величезних об'єктів. Не масштабується при зміні масштабу (Zoom). |
| **Blazor WASM (UI Thread)** | Спільна пам'ять, простота розробки. | Блокує UI при дуже важких обчисленнях (>100мс), навіть з `Task.Run` (Single Thread limitations). |
| **Integer Coordinates** | Відсутність помилок округлення, детермінізм. | Неможливість субпіксельної точності (важливо для Zoom > 1000%). |

### 9.2. Roadmap for Scale

Хоча поточна версія стабільно працює з **300k-500k** об'єктів (завдяки *Render Decimation* та *Uniform Grid*), для масштабування до **мільйонів** об'єктів або досягнення **ідеальної плавності (Zero Freeze)** заплановано:

#### Phase 1: Advanced Spatial Partitioning
*   **QuadTree / R-Tree**: Заміна Uniform Grid на адаптивні структури даних (QuadTree). Це вирішить проблему продуктивності при нерівномірному розподілі ліній (наприклад, щільна міська забудова vs пусте поле).
*   **SIMD Optimization**: Використання `System.Runtime.Intrinsics` (Vector<T>) для паралельної обробки координат точок. Це може прискорити трансформацію (Rotate/Translate) у 4-8 разів.

#### Phase 2: True Multithreading
*   **Web Workers**: Винесення `IntersectionService` у справжній окремий потік (Web Worker).
    *   *Виклик*: Потребує серіалізації даних (SharedArrayBuffer) між UI та Worker.
    *   *Вигода*: Гарантована відсутність "фризів" інтерфейсу незалежно від навантаження.

#### Phase 3: Hardware Acceleration
*   **WebGL / WebGPU**: Перенесення рендерингу та, можливо, частини обчислень (Compute Shaders) на GPU.
    *   Поточний Canvas 2D API стає вузьким місцем при малюванні >50k примітивів.
    *   GPU дозволить малювати мільйони ліній за 16 мс (60 FPS).

### 9.3. Conclusion

Поточна архітектура проєкту демонструє високу ефективність, стабільно обробляючи сцени до **500 000 ліній** (завдяки Uniform Grid та Render Budget). Вона балансує між складністю коду, швидкістю розробки та продуктивністю. Впроваджені патерни (Clean Architecture, MVVM) та алгоритми (Spatial Hashing, Two-Phase Detection) створюють надійний фундамент для подальшого розвитку без необхідності повного переписування коду.

## 10. How to Run

Цей розділ містить інструкції для швидкого запуску та перевірки проєкту локально.

### 10.1. Prerequisites

*   **.NET SDK 8.0+**: Перевірити встановлену версію: `dotnet --version`.
*   **IDE (Optional)**: VS Code, Visual Studio 2022, або JetBrains Rider.
*   **Additional tooling**: Not required (використовується стандартний Blazor WASM пайплайн).

### 10.2. Build & Run

Відкрийте термінал у корені репозиторію та виконайте команди:

```bash
# 1. Відновлення залежностей
dotnet restore

# 2. Збірка проєкту
dotnet build

# 3. Запуск веб-сервера
dotnet run --project src/CarCanvas.Web/CarCanvas.Web.csproj
```

Після успішного запуску відкрийте браузер за адресою:
👉 [http://localhost:5228](http://localhost:5228)

### 10.3. Using the App (Quick Sanity Checklist)

1.  **Initial Load**:
    *   При старті має автоматично завантажитись демо-сцена.
    *   Ви повинні бачити дві машини (синю та зелену) та координатну сітку.
2.  **Add Lines**:
    *   Знайдіть панель "Lines".
    *   Введіть кількість у поле "Generate Random" (наприклад, 100) та натисніть **"Generate"**.
    *   На полотні з'являться чорні лінії.
3.  **Find Intersections**:
    *   У панелі "Car #1 (Blue)" натисніть **"Show Intersections"**.
    *   На перетинах машини з лініями з'являться червоні маркери.
    *   Поле "Intersections" покаже кількість знайдених точок (наприклад, `12`).
4.  **Performance Check**:
    *   Зверніть увагу на поле "Last calc time". Для малих сцен значення зазвичай дуже мале (порядку мілісекунд). Фактичні цифри залежать від пристрою/браузера/сцени і відображаються в UI.
5.  **Debug Info**:
    *   Увімкніть **"Show Debug Boxes"** у панелі ліній.
    *   Ви побачите AABB (Axis-Aligned Bounding Boxes) навколо зон інтересу.
6.  **Stress Test (300k Lines)**:
    *   В панелі "Lines" → "Generate Random": введіть `300000` → натисніть **Generate**.
